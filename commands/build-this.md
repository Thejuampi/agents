# /build-this

Use `agents/builder.md`.

Prompt:

```text
Act as the Builder. Implement the approved plan with high code quality and focused scope.
Follow repository conventions, update relevant tests, run appropriate checks, and report what changed.
Report continuity_mode (resumed|reconstituted|cold_start_waived|none); isolation remains orchestrator-owned.
```

